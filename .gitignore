# Python bytecode / caches
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# Local virtualenvs
.venv/
venv/

# devflow local tool-state (checkpoints, exported Implementation Packets) — never tracked
.devflow/
